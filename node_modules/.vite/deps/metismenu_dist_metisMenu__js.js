import {
  require_jquery
} from "./chunk-MSKAANC7.js";
import {
  __commonJS
} from "./chunk-DZZM6G22.js";

// node_modules/metismenu/dist/metisMenu.js
var require_metisMenu = __commonJS({
  "node_modules/metismenu/dist/metisMenu.js"(exports, module) {
    (function(global, factory) {
      typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory(require_jquery()) : typeof define === "function" && define.amd ? define(["jquery"], factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, global.metisMenu = factory(global.$));
    })(exports, function($) {
      "use strict";
      function _interopDefaultLegacy(e) {
        return e && typeof e === "object" && "default" in e ? e : { "default": e };
      }
      var $__default = _interopDefaultLegacy($);
      const Util = (($2) => {
        const TRANSITION_END = "transitionend";
        const Util2 = {
          // eslint-disable-line no-shadow
          TRANSITION_END: "mmTransitionEnd",
          triggerTransitionEnd(element) {
            $2(element).trigger(TRANSITION_END);
          },
          supportsTransitionEnd() {
            return Boolean(TRANSITION_END);
          }
        };
        function getSpecialTransitionEndEvent() {
          return {
            bindType: TRANSITION_END,
            delegateType: TRANSITION_END,
            handle(event) {
              if ($2(event.target).is(this)) {
                return event.handleObj.handler.apply(this, arguments);
              }
              return void 0;
            }
          };
        }
        function transitionEndEmulator(duration) {
          let called = false;
          $2(this).one(Util2.TRANSITION_END, () => {
            called = true;
          });
          setTimeout(() => {
            if (!called) {
              Util2.triggerTransitionEnd(this);
            }
          }, duration);
          return this;
        }
        function setTransitionEndSupport() {
          $2.fn.mmEmulateTransitionEnd = transitionEndEmulator;
          $2.event.special[Util2.TRANSITION_END] = getSpecialTransitionEndEvent();
        }
        setTransitionEndSupport();
        return Util2;
      })($__default["default"]);
      const NAME = "metisMenu";
      const DATA_KEY = "metisMenu";
      const EVENT_KEY = `.${DATA_KEY}`;
      const DATA_API_KEY = ".data-api";
      const JQUERY_NO_CONFLICT = $__default["default"].fn[NAME];
      const TRANSITION_DURATION = 350;
      const Default = {
        toggle: true,
        preventDefault: true,
        triggerElement: "a",
        parentTrigger: "li",
        subMenu: "ul"
      };
      const Event = {
        SHOW: `show${EVENT_KEY}`,
        SHOWN: `shown${EVENT_KEY}`,
        HIDE: `hide${EVENT_KEY}`,
        HIDDEN: `hidden${EVENT_KEY}`,
        CLICK_DATA_API: `click${EVENT_KEY}${DATA_API_KEY}`
      };
      const ClassName = {
        METIS: "metismenu",
        ACTIVE: "mm-active",
        SHOW: "mm-show",
        COLLAPSE: "mm-collapse",
        COLLAPSING: "mm-collapsing",
        COLLAPSED: "mm-collapsed"
      };
      class MetisMenu {
        // eslint-disable-line no-shadow
        constructor(element, config) {
          this.element = element;
          this.config = {
            ...Default,
            ...config
          };
          this.transitioning = null;
          this.init();
        }
        init() {
          const self2 = this;
          const conf = this.config;
          const el = $__default["default"](this.element);
          el.addClass(ClassName.METIS);
          el.find(`${conf.parentTrigger}.${ClassName.ACTIVE}`).children(conf.triggerElement).attr("aria-expanded", "true");
          el.find(`${conf.parentTrigger}.${ClassName.ACTIVE}`).parents(conf.parentTrigger).addClass(ClassName.ACTIVE);
          el.find(`${conf.parentTrigger}.${ClassName.ACTIVE}`).parents(conf.parentTrigger).children(conf.triggerElement).attr("aria-expanded", "true");
          el.find(`${conf.parentTrigger}.${ClassName.ACTIVE}`).has(conf.subMenu).children(conf.subMenu).addClass(`${ClassName.COLLAPSE} ${ClassName.SHOW}`);
          el.find(conf.parentTrigger).not(`.${ClassName.ACTIVE}`).has(conf.subMenu).children(conf.subMenu).addClass(ClassName.COLLAPSE);
          el.find(conf.parentTrigger).children(conf.triggerElement).on(Event.CLICK_DATA_API, function(e) {
            const eTar = $__default["default"](this);
            if (eTar.attr("aria-disabled") === "true") {
              return;
            }
            if (conf.preventDefault && eTar.attr("href") === "#") {
              e.preventDefault();
            }
            const paRent = eTar.parent(conf.parentTrigger);
            const sibLi = paRent.siblings(conf.parentTrigger);
            const sibTrigger = sibLi.children(conf.triggerElement);
            if (paRent.hasClass(ClassName.ACTIVE)) {
              eTar.attr("aria-expanded", "false");
              self2.removeActive(paRent);
            } else {
              eTar.attr("aria-expanded", "true");
              self2.setActive(paRent);
              if (conf.toggle) {
                self2.removeActive(sibLi);
                sibTrigger.attr("aria-expanded", "false");
              }
            }
            if (conf.onTransitionStart) {
              conf.onTransitionStart(e);
            }
          });
        }
        setActive(li) {
          $__default["default"](li).addClass(ClassName.ACTIVE);
          const ul = $__default["default"](li).children(this.config.subMenu);
          if (ul.length > 0 && !ul.hasClass(ClassName.SHOW)) {
            this.show(ul);
          }
        }
        removeActive(li) {
          $__default["default"](li).removeClass(ClassName.ACTIVE);
          const ul = $__default["default"](li).children(`${this.config.subMenu}.${ClassName.SHOW}`);
          if (ul.length > 0) {
            this.hide(ul);
          }
        }
        show(element) {
          if (this.transitioning || $__default["default"](element).hasClass(ClassName.COLLAPSING)) {
            return;
          }
          const elem = $__default["default"](element);
          const startEvent = $__default["default"].Event(Event.SHOW);
          elem.trigger(startEvent);
          if (startEvent.isDefaultPrevented()) {
            return;
          }
          elem.parent(this.config.parentTrigger).addClass(ClassName.ACTIVE);
          if (this.config.toggle) {
            const toggleElem = elem.parent(this.config.parentTrigger).siblings().children(`${this.config.subMenu}.${ClassName.SHOW}`);
            this.hide(toggleElem);
          }
          elem.removeClass(ClassName.COLLAPSE).addClass(ClassName.COLLAPSING).height(0);
          this.setTransitioning(true);
          const complete = () => {
            if (!this.config || !this.element) {
              return;
            }
            elem.removeClass(ClassName.COLLAPSING).addClass(`${ClassName.COLLAPSE} ${ClassName.SHOW}`).height("");
            this.setTransitioning(false);
            elem.trigger(Event.SHOWN);
          };
          elem.height(element[0].scrollHeight).one(Util.TRANSITION_END, complete).mmEmulateTransitionEnd(TRANSITION_DURATION);
        }
        hide(element) {
          if (this.transitioning || !$__default["default"](element).hasClass(ClassName.SHOW)) {
            return;
          }
          const elem = $__default["default"](element);
          const startEvent = $__default["default"].Event(Event.HIDE);
          elem.trigger(startEvent);
          if (startEvent.isDefaultPrevented()) {
            return;
          }
          elem.parent(this.config.parentTrigger).removeClass(ClassName.ACTIVE);
          elem.height(elem.height())[0].offsetHeight;
          elem.addClass(ClassName.COLLAPSING).removeClass(ClassName.COLLAPSE).removeClass(ClassName.SHOW);
          this.setTransitioning(true);
          const complete = () => {
            if (!this.config || !this.element) {
              return;
            }
            if (this.transitioning && this.config.onTransitionEnd) {
              this.config.onTransitionEnd();
            }
            this.setTransitioning(false);
            elem.trigger(Event.HIDDEN);
            elem.removeClass(ClassName.COLLAPSING).addClass(ClassName.COLLAPSE);
          };
          if (elem.height() === 0 || elem.css("display") === "none") {
            complete();
          } else {
            elem.height(0).one(Util.TRANSITION_END, complete).mmEmulateTransitionEnd(TRANSITION_DURATION);
          }
        }
        setTransitioning(isTransitioning) {
          this.transitioning = isTransitioning;
        }
        dispose() {
          $__default["default"].removeData(this.element, DATA_KEY);
          $__default["default"](this.element).find(this.config.parentTrigger).children(this.config.triggerElement).off(Event.CLICK_DATA_API);
          this.transitioning = null;
          this.config = null;
          this.element = null;
        }
        static jQueryInterface(config) {
          return this.each(function() {
            const $this = $__default["default"](this);
            let data = $this.data(DATA_KEY);
            const conf = {
              ...Default,
              ...$this.data(),
              ...typeof config === "object" && config ? config : {}
            };
            if (!data) {
              data = new MetisMenu(this, conf);
              $this.data(DATA_KEY, data);
            }
            if (typeof config === "string") {
              if (data[config] === void 0) {
                throw new Error(`No method named "${config}"`);
              }
              data[config]();
            }
          });
        }
      }
      $__default["default"].fn[NAME] = MetisMenu.jQueryInterface;
      $__default["default"].fn[NAME].Constructor = MetisMenu;
      $__default["default"].fn[NAME].noConflict = () => {
        $__default["default"].fn[NAME] = JQUERY_NO_CONFLICT;
        return MetisMenu.jQueryInterface;
      };
      return MetisMenu;
    });
  }
});
export default require_metisMenu();
/*! Bundled license information:

metismenu/dist/metisMenu.js:
  (*!
  * metismenu https://github.com/onokumus/metismenu#readme
  * A collapsible jQuery menu plugin
  * @version 3.0.7
  * @author Osman Nuri Okumus <onokumus@gmail.com> (https://github.com/onokumus)
  * @license: MIT 
  *)
*/
//# sourceMappingURL=metismenu_dist_metisMenu__js.js.map
